# ADR-001 시설의 행정구역은 좌표 공간 조인으로 부여

- 상태: 채택
- 맥락: 우체국 찾기의 우편 지역코드(postOffiId)는 행정구역과 1:1 이 아니다(예: 367 = 괴산군, 증평군).
- 결정: 시설 좌표를 `mart.admin_area` 폴리곤에 `ST_Intersects` 로 조인(`CONTAINS`). 해안선 단순화 등으로 밖에 떨어진
  시설은 2km 이내 최근접 지역에 붙이고(`NEAREST`) `SPATIAL_JOIN_MISS` 로 기록한다.
- 대안: 코드↔행정구역 매핑표 수작업 — 유지 불가.
