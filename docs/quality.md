# 품질 검증 — 전 메뉴 점검 방법과 결과

모든 메뉴·API 를 사람이 눌러 보는 대신 **자동으로 반복할 수 있는 점검**으로 확인했습니다. 점검에서 나온 문제는 고친 뒤 같은 점검을
다시 돌려 0건이 되는 것을 확인했습니다. (2026-09-26, 로컬 실데이터 · Chrome · Lighthouse 12)

| 점검 | 방법 | 처음 | 지금 |
|---|---|---|---|
| 단위·통합 테스트 | `make test` (PostGIS 테스트 DB) · `npm test` (vitest) | — | Python 167 · 웹 11 통과 |
| API 퍼징 | 모든 조회·계산 API 에 경계값·비정상 입력 534건 (아주 큰 수·음수·형식 오류·SQL/경로 조작 문자열·유니코드) | **5xx 4건** | 5xx 0건 |
| 화면 상호작용 | 9개 메뉴를 실제 브라우저로 — 지표·단계·시도 바꾸기, 지도 클릭, 레이어 켜기, 탭, 표 행, 복사, What-if·배치 제안 실행 | — | 콘솔 오류·페이지 오류·HTTP 4xx/5xx 0건 (의도한 404 화면 제외) |
| 중복 요청 | 16개 화면 상태(주소 조건 포함)에서 같은 API 를 두 번 부르는지 | **2건** | 0건 |
| 메뉴 이동 요청 수 | 한눈에 → 지도 → 생활 거점 → 지역 순위 → 한눈에 → 지도 | 매번 머리글 3 + 화면별 | 머리글은 처음 1회, 지도 재방문 0개 |
| 접근성 | axe-core (WCAG 2.1 AA) 9개 화면 | 색 대비·`lang`·링크 구분·빈 표 머리글 | **위반 0건** |
| 모바일 | 375px 폭에서 가로 넘침 | — | 0건 |
| 혼합 콘텐츠 | http 로 띄운 화면에서 외부 요청의 스킴 | **평문 24건**(카카오 kakao.js·타일) | 0건 (모두 https) |
| 비밀값·의존성 | gitleaks 전체 이력 · pip-audit · npm audit | 0 | 0 |
| 워커 복원력 | 워커가 도는 중 DB 가 모든 연결을 끊음(`pg_terminate_backend`) | 워커 프로세스 종료 → 재시작 | 차선이 다시 연결해 작업 처리(테스트로 고정) |
| 백업 | `make backup LITE=1` → 임시 DB 로 복원 → 건수·권한 비교 | 절차 없음 | 일치 (원본 응답만 제외) |

## 찾아서 고친 문제

| 문제 | 원인 | 조치 |
|---|---|---|
| `page=99999999999` 등에서 500 (4곳) | 정수 범위를 넘는 값이 DB 형 변환(`bigint`/`smallint`)에서 실패 | 매개변수 범위 검사(`page` 1~100,000, 시설 유형 0~9) + DB `DataError` → 400 `VALIDATION_ERROR` |
| 지역 순위가 같은 요청을 두 번 | 시도 목록이 늦게 와 기본 시도가 정해지면, 시군구 순위와 무관한데도 효과(effect)가 다시 돎 | 요청에 실제로 쓰는 값만 의존 |
| 데이터·운영이 품질 요약을 두 번 | 머리글 배지와 본문이 따로 요청 | 짧은 캐시(5초)로 같은 요청 합치기 |
| 오류 로그 한 건이 로그 창을 덮음 | DB 오류 문자열에 SQL 원문·바인드 값이 포함 | 한 줄 요약(SQL·바인드 값 제거 — 바인드 값은 사용자 입력일 수 있음), 자세한 내용은 traceId 로 서버 로그 |
| 제3자 스크립트를 평문으로 받음 | 카카오 SDK 가 페이지 프로토콜(http)을 따라 본체·타일을 받음 | CSP `upgrade-insecure-requests` + 외부 출처 `https://` 한정 |
| DB 장애 복구 때 워커 전체 재시작 | LISTEN 연결 오류가 차선 스레드를 끝냄 | 차선이 1·2·4…30초 간격으로 다시 연결 |
| 데이터·운영 레이아웃 이동 (CLS 0.194) | 오류 로그 자리 표시(96px)와 실제 목록(최대 360px) 높이 차이 | 로그 창을 고정 높이로 — 불러오는 중·없음·있음 모두 같은 높이 |
| 한눈에 레이아웃 이동 (CLS 0.088) | 방문 여건 배너가 예보를 받은 뒤 숫자 위에 끼어듦 | 배너 자리를 먼저 잡아 둠 |
| 첫 화면 제목이 0.5초 늦게 보임 | 제목 애니메이션이 투명도 0 에서 시작 → 브라우저가 LCP 후보에서 빼 데이터 뒤의 작은 글자가 LCP | 투명도 없이 움직임만 |
| 지도 화면 멈칫함 | 시군구 252곳 폴리곤(좌표 수만 개)을 한 번에 만들어 0.5초 넘는 긴 작업 | 약 12ms 조각으로 나눠 만들고 사이에 브라우저에 양보 |
| 지도 첫 그림이 늦음 | 지도 컴포넌트가 하이드레이션 뒤에 SDK 를 받기 시작 | 지도 화면이 그려지는 즉시 SDK 받기 + SDK 출처 preconnect (지도 없는 화면에서는 받지 않음 — 확인) |
| 입력 칸 이름 없음 (Chrome 이슈) | `select`·`input` 에 `id`/`name` 없음 | `name` 지정 |
| 방문 여건 출처 `에어코리아 발표 —` | 미세먼지 예보는 모레까지만 발표 | "아직 이 날짜까지 발표되지 않음" |

## Lighthouse (모바일 기본 설정: 느린 4G·CPU 4배 감속, 3회 중앙값)

성능 · 접근성 · 모범 사례 · SEO, 그리고 LCP(가장 큰 내용이 그려진 시각)·CLS(레이아웃 이동)·TBT(입력을 막은 시간).

| 화면 | 성능 | 접근성 | 모범 사례 | SEO | LCP | CLS | TBT | 성능 (처음 → 지금) |
|---|---|---|---|---|---|---|---|---|
| 한눈에 `/overview` | 100 | 100 | 100 | 100 | 1.2초 | 0 | 3ms | 99 → 100 |
| 지도 `/` | 91 | 100 | 93 | 100 | 3.1초 | 0 | 174ms | 72 → 91 |
| 방문 여건 `/today` | 92 | 100 | 96 | 100 | 1.7초 | 0 | 330ms | 77 → 92 |
| 생활 거점 `/hubs` | 100 | 100 | 100 | 100 | 1.8초 | 0 | 13ms | 95 → 100 |
| 지역 순위 `/rankings` | 100 | 100 | 100 | 100 | 1.7초 | 0 | 11ms | 95 → 100 |
| What-if `/whatif` | 100 | 100 | 100 | 100 | 1.7초 | 0 | 31ms | 100 |
| 배치 제안 `/plan` | 100 | 100 | 96 | 100 | 1.7초 | 0 | 13ms | 100 |
| 데이터·운영 `/quality` | 100 | 100 | 100 | 100 | 1.8초 | 0.002 | 22ms | 89 → 100 |
| 지표 정의 `/about/metrics` | 94 | 100 | 100 | 100 | 3.0초 | 0.004 | 47ms | 94 |

- 지도가 있는 화면(지도·방문 여건)의 성능 점수는 카카오 SDK 자체의 스크립트 실행과 폴리곤 수에 좌우됩니다.
  지도 화면의 **모범 사례 93~96** 은 로컬이 http 라는 점과 카카오 SDK 가 시도하는 `eval` 을 CSP 로 막은 기록 때문이며,
  `'unsafe-eval'` 을 허용하지 않는 쪽을 택했습니다([ADR-013](adr/ADR-013-api-security.md)).
- 측정은 같은 컴퓨터에서 다른 도커 스택이 함께 도는 상태라 실행마다 ±5점 정도 차이가 있어 3회 중앙값을 적었습니다.

## 다시 돌리기

```bash
make test                         # Python 167개 (워커 차선·DB 끊김 복구·권한·보안 포함)
cd web && npm test                # 웹 11개 (요청 경로 검사·캐시·범례 계산)
npx lighthouse http://localhost:3100/overview --only-categories=performance,accessibility,best-practices,seo
```
