# 벤치마크 기록

`make bench` — 측정 동안만 속도 제한을 끄고(api 재기동), 같은 도커 네트워크의 collector 컨테이너에서 경로마다 동시 16개로
200회(What-if 50회) 요청합니다. uvicorn 워커 2, PostgreSQL 16 + PostGIS 3.6, Redis 7, Apple Silicon 로컬, 전국 실데이터.
끝에 `pg_stat_statements` 누적 시간 상위 SQL 을 함께 출력합니다.

## 2026-09-25 — 개선 전 → 후

| 경로 | p50 (ms) | p95 (ms) | 처리량 (req/s) | 응답 |
|---|---|---|---|---|
| health | 19.6 → 24.6 | 81.6 → 81.5 | 546 → 448 | 스키마·작업 큐 확인이 추가됨 |
| overview (캐시) | 16.7 → 16.6 | 51.8 → 51.7 | 699 → 711 | 3KB |
| **geojson 시군구 (캐시)** | **797 → 49** | 1,254 → 184 | **19 → 223** | 772KB (gzip 약 200KB) |
| geojson 시군구 (ETag 304) | 40.8 → 15.9 | 69.6 → 46.7 | 351 → 785 | 본문 없음 |
| **geojson 읍면동 서울** | **111 → 21** | 157 → 67 | **142 → 554** | 247KB |
| areas 순위 | 26.4 → 22.0 | 67.8 → 60.4 | 501 → 559 | 5KB |
| **area 상세** | **74 → 28** | 110 → 75 | **201 → 462** | 6KB |
| **visit 조건** | **331 → 38** | 490 → 64 | **47 → 392** | 122KB |
| hubs 순위 | 28.2 → 29.9 | 74.8 → 79.0 | 429 → 436 | 6KB |
| **whatif (무작위 1~3곳)** | **193 → 18** | 287 → 56 | **80 → 619** | 3KB |

개별 측정:

| 항목 | 전 → 후 | 방법 |
|---|---|---|
| 읍면동 전국 GeoJSON 생성(캐시 미적중, 4.3MB) | 321 → 70ms | 같은 쿼리를 `json`/`text` 로 5회씩, 중앙값 |
| 기상청 단기예보 수집(격자 241곳) | 288 → 74초 | 작업 큐 실행 기록(ops.job) |

원인과 조치는 [ADR-014](adr/ADR-014-performance.md).

## 화면 (2026-09-26)

| 항목 | 전 → 후 | 방법 |
|---|---|---|
| 메뉴 이동 때 API 요청 (지도 재방문) | 3 → 0개 | 조회 캐시 `cachedApi` — 머리글 3개는 첫 화면에서만 |
| 지역 순위 첫 화면 요청 | `/areas` 2 → 1회 | 효과가 요청에 쓰는 값만 의존 |
| 방문 여건 가장 긴 메인 스레드 작업 (CPU 4배 감속) | 532 → 316ms | 폴리곤 생성을 12ms 조각으로 (`PerformanceObserver` longtask) |
| 지도 LCP (Lighthouse 모바일, 3회 중앙값) | 6.0 → 3.1초 | 카카오 SDK 조기 로드 + preconnect |
| 방문 여건 LCP | 4.3 → 1.7초 | 제목 애니메이션의 투명도 제거 (LCP 후보 복귀) |
| 데이터·운영 CLS | 0.194 → 0.002 | 오류 로그 창 고정 높이 |

전체 화면 점수와 점검 방법은 [품질 검증](quality.md).

## 다시 재기

```bash
make bench              # N=500 C=32 로 바꿀 수 있음
```
